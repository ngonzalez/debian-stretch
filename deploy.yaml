---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kibana
  namespace: effilab
spec:
  selector:
    matchLabels:
      run: debian
  template:
    metadata:
      labels:
        run: debian
    spec:
      volumes:
      - name: cgroup
        hostPath:
          path: /sys/fs/cgroup
          type: Directory
      containers:
      - name: debian
        image: gcr.io/hebe-299715/debian-stretch
        ports:
        - name: ssh
          containerPort: 22
        - name: logstash-tcp
          containerPort: 4000
        - name: logstash-beats
          containerPort: 5044
        - name: logstash-gelf
          containerPort: 12201
        - name: nginx
          containerPort: 8000
        securityContext:
          privileged: true
          capabilities:
            add: ["SYS_ADMIN"]
        volumeMounts:
        - name: cgroup
          mountPath: /sys/fs/cgroup:ro